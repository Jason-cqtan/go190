package main

func main() {
	// go语言中25个关键词
	// package import var const func
	// interface struct map type chan
	// select defer go goto return
	// for if else continue break
	// range switch case default fallthough
}
